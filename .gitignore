# File generati
output/

# Il font non va committato (licenza uso personale) — vedi fonts/README.md
fonts/*.ttf
fonts/*.otf

# Web app (Vite)
webapp/node_modules/
webapp/dist/
webapp/package-lock.json

# Default fonts are downloaded at build time (OFL, Google Fonts), not committed
webapp/public/fonts/*.ttf
webapp/public/fonts/*.otf
